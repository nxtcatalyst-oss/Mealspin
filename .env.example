# Get these from your Neon project dashboard (neon.tech)
# Vercel Postgres tab also provides these automatically when linked

# Pooled connection (used by Prisma at runtime — required)
DATABASE_URL="postgresql://user:pass@host-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Direct connection (used by Prisma for migrations — required with Neon)
DIRECT_URL="postgresql://user:pass@host.region.aws.neon.tech/neondb?sslmode=require"
